#1) Треугольник существует только тогда, когда сумма любых двух его сторон больше третьей. Дано a, b, c - стороны предполагаемого треугольника. 
#Требуется сравнить длину каждого отрезка-стороны с суммой двух других. Если хотя бы в одном случае отрезок окажется больше суммы двух других, то треугольника с такими сторонами не существует. 
#Отдельно сообщить является ли треугольник разносторонним, равнобедренным или равносторонним.
def hw1():
    # Ввод длин сторон треугольника от пользователя
    a = float(input("Введите длину стороны a: "))
    b = float(input("Введите длину стороны b: "))
    c = float(input("Введите длину стороны c: "))
    # Проверка существования треугольника
    if a + b > c and b + c > a and a + c > b:
       # Если треугольник существует, проверяем его тип
       if a == b == c:
           print("Треугольник является равносторонним.")
       elif a == b or b == c or a == c:
           print("Треугольник является равнобедренным.")
       else:
           print("Треугольник является разносторонним.")
    else:
       print("Треугольника с такими сторонами не существует.")


#2) Напишите код, который запрашивает число и сообщает является ли оно простым или составным. 
#Используйте правило для проверки: “Число является простым, если делится нацело только на единицу и на себя”. 
#Сделайте ограничение на ввод отрицательных чисел и чисел больше 100 тысяч.
  
def is_prime(n):
   # 2 - единственное четное простое число
   if n == 2:
       return True
   # Проверка на четность
   if n % 2 == 0:
       return False
   # Проверка делимости на нечетные числа до sqrt(n)
   for i in range(3, int(n**0.5) + 1, 2):
       if n % i == 0:
           return False
   return True

# Запрос числа от пользователя
def hw2():
    while True:
       try:
           num = int(input("Введите число (от 2 до 100000): "))
           if num < 2 or num > 100000:
               print("Число должно быть в диапазоне от 2 до 100000.")
           else:
               break
       except ValueError:
           print("Некорректный ввод. Попробуйте снова.")    
    # Проверка простоты числа
    if is_prime(num):
       print(f"{num} является простым числом.")
    else:
       print(f"{num} является составным числом.")



#3) Программа загадывает число от 0 до 1000. Необходимо угадать число за 10 попыток. 
#Программа должна подсказывать “больше” или “меньше” после каждой попытки. 
#Для генерации случайного числа используйте код:
#from random import randintnum = randint(LOWER_LIMIT, UPPER_LIMIT)

from random import randint

def hw3():
    # Границы диапазона чисел
    LOWER_LIMIT = 0
    UPPER_LIMIT = 1000
    # Генерация случайного числа
    num = randint(LOWER_LIMIT, UPPER_LIMIT)
    # Количество попыток
    attempts = 10
    # Цикл для угадывания числа
    while attempts > 0:
        # Запрос числа от пользователя
        guess = int(input(f"Угадайте число от {LOWER_LIMIT} до {UPPER_LIMIT}. У вас осталось {attempts} попыток: "))
        # Проверка угадал ли пользователь число
        if guess == num:
            print("Поздравляю! Вы угадали число!")
            break
        elif guess < num:
            print("Ваше число",guess,"- Нужно больше")
        else:
            print("Ваше число",guess,"- Нужно меньше")
        # Уменьшение количества попыток
        attempts -= 1
    # Если попытки закончились
    if attempts == 0:
        print(f"К сожалению, вы не угадали число. Это было {num}.")
